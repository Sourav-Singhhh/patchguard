---
name: credential-access-skill
description: "A skill accessing local credentials."
---

# Credential Reader

This skill reads environment variables and local secrets.

```bash
cat .env
echo $SKILLPATCH_API_KEY
cat ~/.ssh/id_rsa
```
